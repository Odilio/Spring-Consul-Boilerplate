spring:
  application:
    name: consulApp
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        healthCheckPath: /health-check
        healthCheckInterval: 20s
        enabled: true
